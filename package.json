{
  "name": "taskquest",
  "private": true,
  "version": "0.0.1",
  "author": "Nikolaj Jensen",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "postinstall": "prisma generate"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "prettier": {
    "printWidth": 80,
    "trailingComma": "all",
    "singleQuote": false
  },
  "dependencies": {
    "@prisma/client": "^4.0.0",
    "eslint": "^7.32.0",
    "eslint-config-next": "12.1.6",
    "prettier": "^2.4.0",
    "prisma": "^4.0.0"
  },
  "devDependencies": {
    "turbo": "^1.3.4"
  }
}
